export interface User {
    email: string;
    password: any;
}

export interface Employee {
    employeeId: string;
    firstName: string;
    lastName: string;
    dob: string;
    address: string;
    telephone: string;
    email: string;
}
